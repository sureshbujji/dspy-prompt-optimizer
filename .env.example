# Copy to .env and fill in. Everything below is OPTIONAL:
# the optimizer, eval, and tests all run offline without any of these.

# --- Real LM (optional) ---------------------------------------------------
# Used only if you swap WeakMockLM for a real model in src/run_optimization.py:
#   dspy.configure(lm=dspy.LM("openai/gpt-4o-mini", api_key=os.environ["OPENAI_API_KEY"]))
OPENAI_API_KEY=<redacted>
